const { assert } = require("chai");
const sinon = require("sinon");
const userContext = require("../../src/data/userContext");

describe("userContext.authenticateUser", function() {
    
});

describe("userContext.registerUser", function() {
    
});